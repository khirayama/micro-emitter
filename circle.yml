general:
  branches:

machine:
  timezone: Asia/Tokyo
  node:
    version: 6.8.0

checkout:
  pre:
    - git config --global user.name "circleci"
    - git config --global user.email "circleci@feedforce.jp"

dependencies:
  override:
    - npm install

test:
  override:
    - npm test
